# Core dependencies
opencv-python>=4.5.0
numpy>=1.21.0
matplotlib>=3.5.0
Pillow>=8.3.0

# Audio processing dependencies
pyaudio>=0.2.11
librosa>=0.9.0
soundfile>=0.10.0

# Speech recognition dependencies
SpeechRecognition>=3.8.0
pyttsx3>=2.90

# Machine Learning dependencies for emotion detection
tensorflow>=2.8.0
deepface>=0.0.79
scikit-learn>=1.0.0
joblib>=1.1.0

# Notes:
# - tkinter and wave are part of the Python standard distribution on Windows and should not be installed via pip.
# - On Windows, if installing PyAudio from PyPI fails, use:
#   pip install pipwin && pipwin install pyaudio
# - TensorFlow and DeepFace may require additional setup. If TensorFlow fails, the app will use alternative methods.
# GUI dependencies
customtkinter>=5.2.0
